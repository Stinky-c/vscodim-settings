{
	// Place your snippets for python here. Each snippet is defined under a snippet name and has a prefix, body and 
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
	// same ids are connected.
	// Example:
	// "Print to console": {
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }
	"nameMain":{
		"prefix": "__main__",
		"body": "if __name__ == \"__main__\":\n\t"
	},
	"load config": {
		"prefix": "loadCFG",
		"body": [
			"import os,json",
			"with open(\"{}/.cfg\".format((os.path.dirname(__file__)).replace('\\\\\\\\',\"/\")),\"r\") as settingF:",
			"\tjsonD$1 = json.load(settingF)",
			"$2"

		],
		"description": "Python: load a .cfg file from the same dir"
	},
	"return dir":{
		"prefix": "returnDir",
		"body": "os.path.dirname(__file__)$1",
		"description": "returns the current dir of the file"

	},
	"open file":{
		"prefix": "loadFile",
		"body": [
			"with open(f\"{$1}\") as f:\n",
			"\tpass"
		]

	},
	"timedateDT": {
		"prefix": "timedate",
		"body": [
			"import datetime as dt ",
			"dt.datetime.today().strftime(\"%Y-%m-%d; %H:%M:%S\")$1"
		],
		"description": "returns the time"
	},
	"timedate": {
		"prefix": "timedate",
		"body": [
			"from time import *",
			"from datetime import *",
			"datetime.utcfromtimestamp($1).strftime(\"%Y-%m-%d; %H:%M:%\")",
			"$1"
		],
		"description": "returns the timestamp"
	},
	"timedate2": {
		"prefix": "timedate2",
		"body": [
			"import time as tm",
			"int(str(tm.time()).split(\".\")[0])",
		],
		"description": "returns time in unix as int "
	},
	"load local .env file":{
		"prefix": "prepENV",
		"body": [
			"import os",
			"from dotenv import load_dotenv",
			"load_dotenv()"
		],
	}

}